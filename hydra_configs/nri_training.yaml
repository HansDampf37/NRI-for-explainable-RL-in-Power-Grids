defaults:
    -   env: case14
    -   model: gnn_dqn
    -   train: default_dqn
    -   obs_space: graph_obs_space
    -   act_space: discrete_set_bus
    -   eval: default
    - _self_

hydra:
    run:
        dir: data/output/${now:%Y-%m-%d_%H-%M-%S}