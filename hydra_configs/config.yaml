defaults:
    -   env: case14
    -   nri: default
    -   rl: default
    -   optuna: default
    - _self_

experiment_name: improve_baseline
timeout: ~

hydra:
    run:
        dir: data/experiments/${experiment_name}/hydra/${now:%Y-%m-%d_%H-%M-%S}
