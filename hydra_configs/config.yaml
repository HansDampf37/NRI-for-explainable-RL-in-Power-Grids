defaults:
    -   env: case14
    -   nri: default
    -   rl: test
    - _self_

experiment_name: fix_eval

hydra:
    run:
        dir: data/experiments/${experiment_name}/hydra/${now:%Y-%m-%d_%H-%M-%S}